type Point = { t: number; score: number };
type Props = { points: Point[]; nowIndex: number };

export function ForecastChart(_props: Props) {
  // TODO Step 7: SVG 4-hour curve, current vertical line, "best in next 4h" marker.
  return null;
}
