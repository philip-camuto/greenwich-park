import type { DemandCategory } from "@/lib/model/types";

type Props = { score: number; category: DemandCategory; updatedAt?: string };

export function DemandScore(_props: Props) {
  // TODO Step 7: massive number, color accent, status line, last-updated.
  return null;
}
